# ══════════════════════════════════════════════════════════════
#  Yandex Business Parser — Dependencies
# ══════════════════════════════════════════════════════════════

# Core
Flask>=3.0
httpx[http2]>=0.27
curl-cffi>=0.16      # HTTP engine with Chrome TLS/JA3 impersonation (anti-bot); httpx used as fallback
requests>=2.31
tqdm>=4.65
colorama>=0.4.6

# Browser automation (Playwright for detail pages, or CDP fallback)
playwright>=1.40
websocket-client>=1.6  # CDP WebSocket for Chrome DevTools Protocol

# Excel export
openpyxl>=3.1

# Maps (HTML)
folium>=0.15

# Geocoding (built-in, uses the http_client engine)
# 2GIS source (built-in, same engine — no extra deps)

# Dev / Testing
# pytest>=7.0
